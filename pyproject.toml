[project]
name = "tsx-gym-booker"
version = "0.1.0"
description = "Automating TSX Gym booking"
requires-python = ">=3.11"
dependencies = { file = ["requirements.txt"] }

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
